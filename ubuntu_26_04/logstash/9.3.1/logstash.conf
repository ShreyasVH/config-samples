input {
  file {
    path => "/home/shreyas/custom.log"
    type => "custom"
  }
}

output {
  elasticsearch {
    hosts => ["https://localhost:1530"]
    index => "%{type}-logs-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "password"
    ssl_certificate_authorities => ["/home/shreyas/workspace/myProjects/ssl/rootCA.crt"]
  }

  stdout { codec => rubydebug }
}
